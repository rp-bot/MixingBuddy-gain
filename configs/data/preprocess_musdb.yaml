musdb:
  root: data/musdb18hq

output:
  root: data/processed/musdb_chunks

chunk:
  sec: 10.0

frame:
  ms: 50.0

gating:
  mixture_min_rms_dbfs: -25.0
  stem_min_rms_dbfs: -40.0
  min_active_frame_ratio: 0.30

anchor:
  fallback_order:
    - bass
    - drums
    - vocals
    - other
